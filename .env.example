# Motsense dashboard environment variables (example)
# Copy to .env.local only if you need to override the default relative API routing.

# Optional. Leave empty in production behind Traefik so browser requests resolve as /api/... and /health.
NEXT_PUBLIC_API_BASE_URL=
